import { vi } from 'vitest';

/**
 * Vitest Setup for @panelcraft-comic-studio/web
 *
 * Currently a plain TypeScript workspace (no React/Next.js dependencies).
 * This file is a placeholder for future integration.
 *
 * TODO: When Next.js 15 is added:
 * 1. Install react, react-dom, next, @types/react, @types/react-dom
 * 2. Re-add @vitejs/plugin-react to vitest.config.ts
 * 3. Uncomment the Next.js/React mocks below
 * 4. Import @testing-library/jest-dom for component testing
 */

/**
 * FUTURE: Next.js & React Mocks (activate when Next.js is installed)
 *
 * vi.mock('next/navigation', () => ({...}));
 * vi.mock('next/image', () => ({...}));
 * vi.mock('next/link', () => ({...}));
 * vi.mock('next/head', () => ({...}));
 */
